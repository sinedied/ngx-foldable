import { IfSpanDirective } from './if-span.directive';

describe('IfSpanDirective', () => {
  it('should create an instance', () => {
    const directive = new IfSpanDirective();
    expect(directive).toBeTruthy();
  });
});
