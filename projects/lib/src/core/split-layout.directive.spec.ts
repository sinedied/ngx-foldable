import { SplitLayoutDirective } from './split-layout.directive';

describe('SplitLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new SplitLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
