import { IfSpanningDirective } from './if-spanning.directive';

describe('IfSpanningDirective', () => {
  it('should create an instance', () => {
    const directive = new IfSpanningDirective();
    expect(directive).toBeTruthy();
  });
});
