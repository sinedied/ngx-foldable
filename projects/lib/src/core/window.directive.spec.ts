import { WindowDirective } from './window.directive';

describe('WindowDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowDirective();
    expect(directive).toBeTruthy();
  });
});
